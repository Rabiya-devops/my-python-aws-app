# Use an official lightweight Python image
FROM python:3.9-slim

# Set the working directory inside the container
WORKDIR /app

# Copy only the requirements file first for better Docker layer caching
COPY requirements.txt .

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the entire application source code to the working directory
COPY . .

# Expose the port the app runs on (Flask default is 5000)
EXPOSE 5000

# Define the command to run the application
# We use Python here, but for production, you might use Gunicorn/uWSGI
CMD ["python", "app.py"]